rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // All reads and writes go through Cloud Functions (Firebase Admin SDK).
    // Admin SDK bypasses these rules, so direct client access is denied entirely.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}